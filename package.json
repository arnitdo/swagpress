{
  "name": "swagpress",
  "version": "1.0.0",
  "description": "NPM Package for generating Swagger/OpenAPI 3.0 documentation on the fly",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "npx tsc && cd test && npx tsc ./*.ts && cd .. && mocha",
    "build": "npx tsc"
  },
  "keywords": [
    "swagger",
    "express",
    "api",
    "docs"
  ],
  "author": "arnitdo",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^9.1.1",
    "@types/node": "^18.14.0",
    "mocha": "^10.0.0",
    "typescript": "^4.8.3"
  }
}
